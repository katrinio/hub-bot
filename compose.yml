services:
  hub-bot:
    build: .
    image: the-hub-bot:latest
    container_name: the-hub-bot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
